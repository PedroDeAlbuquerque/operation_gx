/// @description Handle the panda selection npc update

if obj_game_character_selection_controller.character_selection_player_kind == player_kind.panda {
	image_blend = c_white;
} else {
	image_blend = c_dkgray;
}